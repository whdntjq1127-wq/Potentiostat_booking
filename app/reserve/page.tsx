export default function ReservePage() {
  return (
    <main className="lookup-layout">
      <section className="panel">
        <div className="eyebrow">Notice</div>
        <h1 className="section-title">Create bookings from the main weekly board.</h1>
        <p className="muted">
          Click a calendar slot on the home page to create a booking directly on the
          same screen.
        </p>
      </section>
    </main>
  );
}
